import { FontWeight } from "../../types/theme"

export const IBM_PLEX_SANS_FONT_FAMILY = 'IBM Plex Sans'

export const FONT_WEIGHTS: Record<FontWeight, number> = {
	bold: 700,
	medium: 600,
	regular: 400,
	light: 300,
	extralight: 200,
}

export const PROJECTS_TITLE = 'Projetos'
